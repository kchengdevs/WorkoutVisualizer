# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Workout Visualizer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Workout Visualizer

  

  target 'Workout VisualizerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Workout VisualizerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  pod 'SDWebImage/WebP'
end
